// Session setup — custom samples and synths for the sandbox session.
// Runs once in the browser on connect. See sessions/_template/setup.js for
// examples (samples(...) / registerSound(...)). Empty for now.
